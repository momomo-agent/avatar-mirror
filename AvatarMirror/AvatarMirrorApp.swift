import SwiftUI
import os

private let logger = Logger(subsystem: "com.momomo.avatar-mirror", category: "app")

@main
struct AvatarMirrorApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
